make a file called "credentials.json"
in it, have the following contenent (including the {} )

{
	"client_id": "the clent id from developer.spotify.com dashboard",
	"client_secret": "the clent id from developer.spotify.com dashboard"
}